BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[products]
ADD [image_url] NVARCHAR(1000),
    [image_key] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
